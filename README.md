# MA-Project
Antes de ejecutar npx tenemos que ejecutar lo siguiente: npm run api

# Dependencias para descargar
npx expo install expo-location
npx expo install react-native-maps
npx expo install expo-image-picker
